# Exemplo de variáveis de ambiente para a API
# Copie para server/.env e preencha com valores reais (NÃO COMMITAR)

# Porta do servidor (Render define automaticamente via PORT)
# PORT=4000

# URI do MongoDB (Atlas)
MONGODB_URI=mongodb+srv://<usuario>:<senha>@<cluster>/<db>?retryWrites=true&w=majority

# Segredo JWT (use string forte; gere um novo e NÃO compartilhe)
JWT_SECRET=troque-por-um-segredo-forte

# CORS permitido (separado por vírgula). Ex.: https://seu-site.vercel.app,https://*.vercel.app
# CORS_ORIGIN=
